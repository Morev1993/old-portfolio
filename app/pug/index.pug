include ./inc/head.pug
body
  include ./inc/aside.pug

  main
    .main-page
      .wrapper
        .bg
          .content
            h1.typist
            div.more
              a(href="#" class="btn btn-danger") Подробнее

  include ./inc/footer.pug